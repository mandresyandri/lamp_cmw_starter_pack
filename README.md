# How it work ? 
> Running a lamp server...

## Build env 

``` batch
docker-compose build
```
``` batch
docker-compose
```

Be sure that you are in 

``` batch
mkdir lamp-docker && cd lamp-docker
```